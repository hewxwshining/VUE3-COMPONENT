/**
 * @file 公共方法
 * @author xzh
 * @date 2019-09-25 16:10:51
 */

export const defineComponent = (name) => {
    return (component) => {
        // 可以先传组件名也可以先传组件
        if (typeof name !== 'string') {
            [component, name] = [name, component]
        }

        customElements.define(`w-${name}`, component)

        return component
    }
}
